# Build the venv inside the image; never ship the host one.
.venv/

# VCS / caches
.git/
.gitignore
__pycache__/
*.py[oc]

# Secrets must come from Railway variables, not the image.
.env
.env.*
!.env.example

# Local/dev artifacts
*.pdf
Screenshot*
README.md
